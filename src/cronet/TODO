- Response object
- Python Callbacks 
- Reference counts and error handling in c module
- Handle shutdown gracefully
- Async support
- Canceling of requests
- Proxy support
- Raise errors as exceptions
- Timeouts


with cronet.Cronet() as engine:
    Request()
    response = engine.get()
